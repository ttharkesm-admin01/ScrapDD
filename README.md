# ระบบขายเศษวัสดุเหลือใช้ — TKF

หน้าเว็บอยู่บน **GitHub Pages** (public เสมอ) ส่วนข้อมูล รหัสผ่าน และการตรวจสิทธิ์ทั้งหมดอยู่ที่
**Google Apps Script + Google Sheets + Google Drive** ซึ่งเป็นบัญชีของคุณเท่านั้น

```
เบราว์เซอร์ ── fetch ──▶ Apps Script (ตรวจ token + บทบาท) ──▶ Sheets / Drive
   (GitHub Pages)                 ▲ ข้อมูลอยู่ฝั่งนี้ทั้งหมด
```

---

## ติดตั้ง 1 ครั้ง

### 1. สร้างหลังบ้าน

1. สร้าง Google Sheet ใหม่ในบัญชีธุรการ ตั้งชื่อ `TKF-ScrapSales-DB`
2. เมนู **ส่วนขยาย → Apps Script**
3. ลบโค้ดเดิมทิ้ง แล้ววางเนื้อหาไฟล์ `backend/Code.gs`
4. กด ⚙️ **การตั้งค่าโปรเจกต์ → แสดงไฟล์ appsscript.json** แล้ววางเนื้อหาไฟล์ `backend/appsscript.json`
5. เลือกฟังก์ชัน `setup` แล้วกด **เรียกใช้** → อนุญาตสิทธิ์ → จะขึ้นกล่องบอกรหัสผ่านเริ่มต้น

ระบบจะสร้างชีต `users / sessions / fields / records / files / audit` ใส่หัวข้อ 26 รายการจาก AppSheet เดิม
และสร้างบัญชี `admin` / `ChangeMe2026!`

### 2. Deploy เป็นเว็บแอป

**ทำให้เข้าถึงได้ → ติดตั้ง → ประเภท: เว็บแอป**

| ช่อง | ต้องเลือก |
|---|---|
| ดำเนินการในฐานะ | **ฉัน (บัญชีธุรการ)** |
| ผู้มีสิทธิ์เข้าถึง | **ทุกคน** |

> "ทุกคน" หมายถึงเรียก URL ได้โดยไม่ต้องมีบัญชี Google — นี่คือสิ่งที่ทำให้ login แบบธรรมดาเป็นไปได้
> ความปลอดภัยมาจาก token ในโค้ด ไม่ใช่จากการซ่อน URL

คัดลอก URL ที่ลงท้าย `/exec` เก็บไว้

### 3. ขึ้น GitHub Pages

1. สร้าง repo ใหม่ (private ก็ได้ แต่เว็บที่ publish จะเป็น public เสมอ)
2. แก้ `app.js` บรรทัดบนสุด ใส่ URL ที่ได้จากข้อ 2

```js
API: 'https://script.google.com/macros/s/AKfycb.../exec',
```

3. push ไฟล์ `index.html` กับ `app.js` (ไม่ต้องขึ้นโฟลเดอร์ `backend/`)
4. **Settings → Pages → Source: Deploy from a branch → main / (root)**
5. เปิดเว็บที่ได้ → เข้าด้วย `admin` → เข้าเมนู **ตั้งค่า** → เปลี่ยนรหัสผ่านทันที → เพิ่มผู้ใช้จริง

### 4. ตั้ง trigger ล้าง session (แนะนำ)

Apps Script → นาฬิกา (Triggers) → เพิ่ม → ฟังก์ชัน `cleanupSessions` → รายวัน

---

## บทบาทและสิทธิ์ (ตรวจที่ฝั่ง Apps Script)

| บทบาท | อ่าน | กรอก/แก้ | ส่งตรวจ | ตรวจสอบ | อนุมัติ | จัดการหัวข้อ/ผู้ใช้ |
|---|:-:|:-:|:-:|:-:|:-:|:-:|
| `admin` | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| `staff` | ✓ | ✓ | ✓ | | | |
| `supervisor` | ✓ | | | ✓ | | |
| `manager` | ✓ | | | | ✓ | |
| `viewer` | ✓ | | | | | |

**เส้นทางสถานะ** — แท็บบนหน้าแรกคือมุมมองของสถานะเหล่านี้

```
ร่าง ──ส่งตรวจ──▶ รอตรวจสอบ ──ตรวจผ่าน──▶ รออนุมัติ ──อนุมัติ──▶ สมบูรณ์
  ▲                    │                        │
  └──── ตีกลับ ◀───────┴────────────────────────┘
```

- แก้ไขได้เฉพาะสถานะ **ร่าง** และ **ตีกลับ** เท่านั้น (admin ข้ามได้)
- ผู้ตรวจ/ผู้อนุมัติ **แก้ข้อมูลไม่ได้** เซ็นได้อย่างเดียว
- ค่าเริ่มต้นห้ามเซ็นเอกสารที่ตัวเองเป็นผู้จัดทำ — เปลี่ยนได้ที่ `ALLOW_SELF_APPROVE` ใน `Code.gs`
- ค่าเริ่มต้น **ส่งตรวจได้แม้กรอกไม่ครบหรือรูปไม่ครบ** — เครื่องหมาย `*` ในฟอร์มเป็นแค่คำแนะนำ
  ถ้าต้องการบังคับให้กรอกครบก่อนส่ง ตั้ง `REQUIRE_COMPLETE_ON_SUBMIT = true` ใน `Code.gs`
- ลายเซ็น 3 ช่อง (จัดทำ/ตรวจสอบ/รับทราบ) ระบบประทับชื่อ+เวลาให้เองตอนกดปุ่ม ไม่ต้องพิมพ์

---

## หน้าตาและการใช้งาน

ออกแบบด้วย Claude Design (ทิศทาง "กรอกทีละขั้น") — พร้อมต์ที่ใช้อยู่ที่ `docs/claude-design-prompt.md`

**หน้ากรอกเอกสารเป็นแบบทีละขั้น** จำนวนขั้นมาจากจำนวน `section` ในชีต `fields`
เพิ่ม/ลบกลุ่มหัวข้อแล้วจำนวนขั้นเปลี่ยนตามเอง ไม่ต้องแก้โค้ด ขั้นสุดท้ายคือ **ตรวจทานก่อนส่ง**
ที่รวมความครบของทุกกลุ่ม ลายเซ็น 3 ช่อง และปุ่มดำเนินการตามบทบาท

- ลูกศรบนหัว = ออกจากเอกสาร · ปุ่ม "ย้อนกลับ" ที่แถบล่าง = ถอยทีละขั้น
- กดที่แถบขั้นด้านบน หรือกดรายการในหน้าตรวจทาน เพื่อกระโดดไปกลุ่มไหนก็ได้
- ผู้ตรวจ/ผู้อนุมัติเปิดเอกสารมาที่หน้าตรวจทานเลย ไม่ต้องไล่ทีละขั้น

**อัปโหลดรูปเบื้องหลัง** — กดถ่ายรูปถัดไปได้ทันทีโดยไม่ต้องรอรูปก่อนหน้าอัปเสร็จ
ระบบทยอยส่งทีละไฟล์ตามคิว พร้อมแสดงเปอร์เซ็นต์จริงจากขาส่ง
(ทยอยทีละไฟล์เพราะหลังบ้านลบแถวทะเบียนไฟล์ตามเลขดัชนี ยิงพร้อมกันแล้วดัชนีจะเลื่อนชนกัน)

**แดชบอร์ด** — แท็บที่สองข้างบน สรุปยอดที่ฝั่งเซิร์ฟเวอร์ กรองตามปี/เดือน/รายการสินค้า/ซัพพลายเออร์

| ส่วน | ที่มาของข้อมูล |
|---|---|
| ใบงานทั้งหมด · น้ำหนักรวม | นับใบงาน และรวมค่าในช่อง `f_qty` |
| รอดำเนินการ · ค้างเกิน 7 วัน | ใบที่สถานะรอตรวจสอบ/รออนุมัติ นับจาก `updated_at` |
| น้ำหนักขายรายเดือน | 6 เดือนล่าสุดตามวันที่เอกสาร |
| สถานะใบงาน · อายุใบงาน | สัดส่วนตามสถานะ และช่วงอายุ 0–3 / 4–7 / เกิน 7 วัน |
| ซัพพลายเออร์ · รายการสินค้า | 5 อันดับตามน้ำหนัก |

> ยังไม่มีช่อง "หมวดเศษวัสดุ" แยกต่างหาก แดชบอร์ดจึงใช้ช่อง **รายการสินค้า** (`f_item`) แทน
> ถ้าต้องการหมวดจริง เพิ่มหัวข้อชนิด `select` แล้วแก้ `FIELD_ITEM` ใน `Code.gs`

## พิมพ์แบบฟอร์ม

กดปุ่ม **🖨 พิมพ์** ที่หน้าตรวจทาน — ได้แบบฟอร์ม A4 "รูปภาพแสดงขั้นตอนการขายผลพลอยได้" ทันที
**พิมพ์ได้ทุกสถานะ ไม่ต้องรอส่งตรวจหรืออนุมัติ** ขอแค่กดบันทึกใบงานแล้ว

- หัวกระดาษ ผู้ลงนาม และหน่วยน้ำหนัก แก้ได้ที่ค่า `PRINT` บนหัวไฟล์ `app.js`
- ข้อมูลหัวเอกสาร = ช่องที่ไม่ใช่รูปทั้งหมด เรียงตามลำดับในฟอร์ม
  ช่องที่ข้อความยาวเกิน 38 ตัวอักษรจะกินเต็มแถวเอง ไม่ถูกตัดหายในคอลัมน์แคบ
- กลุ่มรูป = ทุกกลุ่มที่เป็นรูปภาพ แถวละ 4 รูปตามแบบฟอร์มเดิม
- **ไม่พิมพ์กลุ่ม "เอกสารแนบ"** เพราะใบนี้ใช้เซ็นก่อนรถออกนอกโรงงานแล้วส่งบัญชี
  ตอนนั้นเอกสารแนบยังไม่ครบ — เปลี่ยนได้ที่ `PRINT.skipSections` ใน `app.js`
  (ชื่อต้องตรงกับชื่อกลุ่มในชีต `fields` เป๊ะ ถ้าเปลี่ยนชื่อกลุ่มต้องมาแก้ตรงนี้ด้วย)
- ดึงภาพเต็มทีละ 4 รูปพร้อมแสดงความคืบหน้า (ยิงทีเดียว 12 รูปทำให้คำตอบใหญ่เกินและช้ามาก)
- บนมือถือหน้าตัวอย่างย่อให้พอดีจอ ตอนสั่งพิมพ์จริงกลับเป็นขนาด A4 เต็ม

## เลขที่เอกสาร

รูปแบบ `ปีเดือนวัน-เลขรัน` เช่น `20260914-001` — เลขรันเริ่มนับ 1 ใหม่ทุกวัน (เวลาไทย)
เอกสารเก่าที่สร้างก่อนหน้านี้ยังคงเลขเดิมแบบ `SC2026-0001` ไว้ ไม่ถูกแก้ย้อนหลัง

## เพิ่ม / ลบ / ซ่อน หัวข้อเอง

เมนู **ตั้งค่า → หัวข้อในแบบฟอร์ม** ทำได้ทันทีโดยไม่ต้องแก้โค้ดหรือแก้โครงชีต
เพราะค่าทั้งหมดเก็บเป็น JSON ก้อนเดียวในคอลัมน์ `data_json` ไม่ใช่คอลัมน์ตายตัวแบบ AppSheet

- **ซ่อน** = ไม่แสดงในฟอร์ม แต่ข้อมูลเดิมยังอยู่ครบ — ใช้กับหัวข้อที่เลิกใช้แต่ยังต้องดูย้อนหลัง
- **วัน/เดือน/ปี** และ **ซัพพลายเออร์** ลบไม่ได้ **และซ่อนไม่ได้** เพราะระบบใช้สองช่องนี้ค้นหาและกรอง
  (ซ่อนไว้ = ไม่มีช่องให้กรอก ค่าที่บันทึกจะว่าง แล้วตัวกรองปี/เดือนก็ใช้ไม่ได้ ผลเท่ากับลบทิ้ง) — ดู `PROTECTED_FIELDS` ใน `Code.gs`
- **ลบ** = ลบแค่นิยามหัวข้อ ค่าที่กรอกไว้ยังอยู่ในชีต สร้างหัวข้อรหัสเดิมใหม่ก็กลับมาแสดง
- **ในตาราง** = ให้หัวข้อนี้ขึ้นเป็นคอลัมน์ในหน้าแรก
- ชนิดที่รองรับ: `text` `number` `date` `select` `textarea` `image`

---

## เรื่องความเร็ว (ปัญหาหลักของ AppSheet เดิม)

| จุด | วิธีที่ใช้ |
|---|---|
| หน้ารายการ | ส่งเฉพาะข้อความ **ไม่ส่งรูปเลย** แสดงแค่จำนวนรูป |
| อัปโหลด | ย่อรูปด้วย canvas ในเครื่องก่อนส่ง (เต็ม 1400px / ย่อ 320px) เหลือไฟล์ละ ~150KB |
| เปิดเอกสาร | ดึงภาพย่อด้วยคำขอเดียว (`files.batch`) และดึงเฉพาะรูปที่ยังไม่มีในเครื่อง |
| รูปที่เพิ่งถ่าย | ใช้ภาพย่อที่ย่อในเครื่องแสดงเลย ไม่ต้องดึงกลับจาก Drive |
| ตรวจ session | แคช session/ผู้ใช้ไว้ ไม่ต้องไล่อ่านทั้งชีตทุกคำขอ |
| ภาพเต็ม | โหลดเมื่อกดดูทีละรูปเท่านั้น |

ประมาณการ: 20 รายการ/เดือน × 20 รูป ≈ **70 MB/เดือน** บน Drive

---

## ข้อจำกัดที่ควรรู้ (ไม่ใช่บั๊ก แต่ต้องรู้ไว้)

1. **GitHub Pages เป็น public** — ป้องกันได้แค่ข้อมูล ไม่ใช่ตัวหน้าเว็บ ใครก็เปิดหน้า login ได้ แต่เข้าไม่ได้ถ้าไม่มีรหัส
2. **CORS ล็อกโดเมนไม่ได้** — Apps Script ไม่ให้กำหนด `Access-Control-Allow-Origin` เอง ใครก็ยิง API ได้ แต่ไม่มี token ก็ทำอะไรไม่ได้ (จึงต้องตั้งรหัสผ่านให้ยาว)
3. **รหัสผ่านใช้ SHA-256 + salt 1000 รอบ** ไม่ใช่ bcrypt เพราะ Apps Script ไม่มีให้ — เพียงพอสำหรับระบบภายใน 3–5 คน แต่ห้ามใช้รหัสซ้ำกับบัญชีอื่น
4. **ลบรูปในฟอร์ม** ตัดการเชื่อมโยงเท่านั้น ไฟล์เดิมยังอยู่ใน Drive (ลบทั้งเอกสารถึงจะลบไฟล์จริง)
   แต่ **กด "เปลี่ยน" รูป** ไฟล์รุ่นก่อนจะถูกเก็บกวาดตอนอัปโหลดครั้งถัดไปของช่องนั้น
   (ตั้งใจให้ช้าไป 1 รุ่น เพราะถ้าลบไฟล์ที่เอกสารยังอ้างถึงแล้วบันทึกไม่ผ่าน รูปจะหายทันที)
5. **ลบแถวใน `sessions` ด้วยมือเพื่อเตะคนออก** ไม่มีผลทันที เพราะ session ถูกแคชไว้ ~25 นาที
   ถ้าต้องตัดสิทธิ์เดี๋ยวนั้นให้ **ปิดใช้งานบัญชี** ในหน้าตั้งค่าแทน (ล้างแคชให้ทันที)
6. **โควตา Apps Script** ~20,000 การเรียก/วัน — ที่ปริมาณงานจริงถือว่าเหลือเฟือ
7. **ยังไม่มี** ออกรายงาน PDF / dashboard กราฟ — เป็นงานเฟสถัดไป ข้อมูลรองรับไว้แล้ว

---

## โครงไฟล์

```
index.html          หน้าเว็บ + สไตล์      ← ขึ้น GitHub Pages
app.js              ตรรกะฝั่งหน้าเว็บ      ← ขึ้น GitHub Pages
backend/Code.gs     หลังบ้านทั้งหมด        ← วางใน Apps Script
backend/appsscript.json                   ← วางใน Apps Script
```

## ทดสอบก่อนใช้จริง (ยังไม่ได้รัน — ต้องทำเองหลัง deploy)

- [ ] เข้าระบบด้วยรหัสผิด 5 ครั้ง → ต้องถูกล็อก (ค่าปัจจุบัน 3 วินาที ตั้งที่ `LOCKOUT_SECONDS`
      ใช้งานจริงควรปรับเป็น 60 ขึ้นไป ไม่งั้นเพดาน 5 ครั้งกันการเดารหัสไม่ได้)
- [ ] โดนล็อกแล้วอยากปลดทันที → รันฟังก์ชัน `unlock` ใน Apps Script
- [ ] เปิด URL `/exec` ตรงๆ ในเบราว์เซอร์ → ต้องได้ JSON เปล่า ไม่ใช่ข้อมูลเอกสาร
- [ ] login เป็น `supervisor` แล้วลองกดแก้ข้อมูล → ต้องแก้ไม่ได้
- [ ] เอกสารสถานะ "สมบูรณ์" → ปุ่มบันทึกต้องหาย และแก้ผ่าน API ตรงๆ ต้องถูกปฏิเสธ
- [ ] คัดลอก Drive ID ของรูปมาเรียก `files.get` โดยไม่ใส่ token → ต้องถูกปฏิเสธ
- [ ] สร้างหัวข้อใหม่ 1 อัน → ต้องขึ้นในฟอร์มทันทีโดยไม่ต้องแก้โค้ด
- [ ] กดลบหัวข้อ "วัน/เดือน/ปี" → ปุ่มต้องกดไม่ได้ และถ้าเรียก API ตรงๆ ต้องถูกปฏิเสธ
- [ ] ตีกลับเอกสารจากสถานะ "รออนุมัติ" → ลายเซ็นช่องตรวจสอบต้องถูกล้างเป็น —
- [ ] แก้ชื่อหัวข้อที่ซ่อนไว้ → ต้องยังซ่อนอยู่เหมือนเดิม
- [ ] เปิดฟอร์มค้างไว้จนเกินอายุ session แล้วกดบันทึก → ต้องเด้งเข้าหน้า login และข้อมูลที่กรอกต้องกลับมาหลังเข้าใหม่
- [ ] มี staff สองคน → คนที่สองต้องกดส่งตรวจเอกสารของคนแรกไม่ได้
- [ ] ปิดใช้งานผู้ใช้คนหนึ่ง แล้วกด "เปลี่ยนบทบาท" ของคนเดิม → ต้องยังปิดอยู่ ไม่ถูกเปิดกลับมาเอง
- [ ] ใส่บทบาทที่สะกดผิด (เช่น `Manager`) → ต้องถูกปฏิเสธ ไม่ใช่สร้างบัญชีที่เข้าได้แต่ทำอะไรไม่ได้
- [ ] ลองเปลี่ยนบทบาทหรือปิดบัญชี admin คนสุดท้าย → ต้องถูกปฏิเสธ
- [ ] กดลองซ่อนหัวข้อ "วัน/เดือน/ปี" → ปุ่มต้องกดไม่ได้ และเรียก API ตรงๆ ต้องถูกปฏิเสธ
- [ ] กางกลุ่มหัวข้อกลุ่มที่สอง แล้วอัปโหลดรูป → กลุ่มที่กางไว้ต้องยังกางอยู่ทั้งหมด
- [ ] แนบรูปแล้วกด "เปลี่ยน" เป็นรูปใหม่ → ชีต `files` ต้องเหลือ 2 แถวของช่องนั้น ไม่ใช่ 4
- [ ] ปิดบัญชีผู้ใช้ที่กำลังเปิดหน้าเว็บค้างไว้ → คำขอถัดไปของเขาต้องเด้งออกทันที ไม่ต้องรอแคชหมดอายุ
- [ ] ถ่ายรูป 3 รูปติดกันเร็วๆ โดยไม่รอ → ต้องขึ้นคิวอัปครบทั้ง 3 ไม่มีรูปไหนหาย
- [ ] อัปโหลดค้างอยู่แล้วกดออกจากเอกสาร → ต้องเตือนว่ายังไม่ได้บันทึก
- [ ] เพิ่มกลุ่มหัวข้อใหม่ 1 กลุ่ม → จำนวนขั้นในฟอร์มต้องเพิ่มเป็นขั้นถัดไปเองโดยไม่ต้องแก้โค้ด
- [ ] login เป็น supervisor แล้วเปิดเอกสารที่รอตรวจสอบ → ต้องเปิดมาที่หน้าตรวจทานเลย และแก้ข้อมูลไม่ได้
- [ ] แดชบอร์ดกรองเดือนที่ไม่มีข้อมูล → ต้องขึ้น "ไม่มีข้อมูลในช่วงที่เลือก" พร้อมปุ่มล้างตัวกรอง
- [ ] สร้างใบงานใหม่ยังไม่บันทึก แล้วกดพิมพ์ → ต้องเตือนให้บันทึกก่อน
- [ ] ใบงานสถานะ "ร่าง" → ปุ่มพิมพ์ต้องกดได้ ไม่ถูกบังคับให้ส่งตรวจก่อน
- [ ] สั่งพิมพ์จากมือถือ → กระดาษต้องออกเต็มหน้า A4 ไม่ใช่ขนาดย่อตามที่เห็นบนจอ
- [ ] ใบงานที่ยังไม่มีรูปเลย → ช่องรูปต้องเป็นกรอบว่าง ไม่ค้างที่ "กำลังโหลด…" และไม่มีข้อความใดๆ
- [ ] สั่งพิมพ์ใบงานที่แนบเอกสารครบ → ใบพิมพ์ต้องไม่มีหัวข้อ "เอกสารแนบ" และรูปในกลุ่มนั้น
